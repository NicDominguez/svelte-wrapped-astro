/* generated by Svelte v3.42.3 */
import { SvelteComponent, binding_callbacks, create_slot, detach, element, flush, get_all_dirty_from_scope, get_slot_changes, init, insert, listen, safe_not_equal, set_custom_element_data, transition_in, transition_out, update_slot_base } from "svelte/internal";
import { createEventDispatcher, onMount } from 'svelte';
function create_fragment(ctx) {
    let rux_segmented_button;
    let current;
    let mounted;
    let dispose;
    const default_slot_template = /*#slots*/ ctx[7].default;
    const default_slot = create_slot(default_slot_template, ctx, /*$$scope*/ ctx[6], null);
    return {
        c() {
            rux_segmented_button = element("rux-segmented-button");
            if (default_slot)
                default_slot.c();
            set_custom_element_data(rux_segmented_button, "selected", /*selected*/ ctx[0]);
        },
        m(target, anchor) {
            insert(target, rux_segmented_button, anchor);
            if (default_slot) {
                default_slot.m(rux_segmented_button, null);
            }
            /*rux_segmented_button_binding*/ ctx[8](rux_segmented_button);
            current = true;
            if (!mounted) {
                dispose = listen(rux_segmented_button, "rux-change", /*onEvent*/ ctx[2]);
                mounted = true;
            }
        },
        p(ctx, [dirty]) {
            if (default_slot) {
                if (default_slot.p && (!current || dirty & /*$$scope*/ 64)) {
                    update_slot_base(default_slot, default_slot_template, ctx, 
                    /*$$scope*/ ctx[6], !current
                        ? get_all_dirty_from_scope(/*$$scope*/ ctx[6])
                        : get_slot_changes(default_slot_template, /*$$scope*/ ctx[6], dirty, null), null);
                }
            }
            if (!current || dirty & /*selected*/ 1) {
                set_custom_element_data(rux_segmented_button, "selected", /*selected*/ ctx[0]);
            }
        },
        i(local) {
            if (current)
                return;
            transition_in(default_slot, local);
            current = true;
        },
        o(local) {
            transition_out(default_slot, local);
            current = false;
        },
        d(detaching) {
            if (detaching)
                detach(rux_segmented_button);
            if (default_slot)
                default_slot.d(detaching);
            /*rux_segmented_button_binding*/ ctx[8](null);
            mounted = false;
            dispose();
        }
    };
}
function instance($$self, $$props, $$invalidate) {
    let { $$slots: slots = {}, $$scope } = $$props;
    let __ref;
    let __mounted = false;
    const dispatch = createEventDispatcher();
    let { data = undefined } = $$props;
    let { selected = undefined } = $$props;
    const getWebComponent = () => __ref;
    onMount(() => {
        $$invalidate(5, __mounted = true);
    });
    const setProp = (prop, value) => {
        if (__ref)
            $$invalidate(1, __ref[prop] = value, __ref);
    };
    const onEvent = e => {
        e.stopPropagation();
        dispatch(e.type, e.detail);
    };
    function rux_segmented_button_binding($$value) {
        binding_callbacks[$$value ? 'unshift' : 'push'](() => {
            __ref = $$value;
            $$invalidate(1, __ref);
        });
    }
    $$self.$$set = $$props => {
        if ('data' in $$props)
            $$invalidate(3, data = $$props.data);
        if ('selected' in $$props)
            $$invalidate(0, selected = $$props.selected);
        if ('$$scope' in $$props)
            $$invalidate(6, $$scope = $$props.$$scope);
    };
    $$self.$$.update = () => {
        if ($$self.$$.dirty & /*__mounted, data*/ 40) {
            $: if (__mounted)
                setProp('data', data);
        }
    };
    return [
        selected,
        __ref,
        onEvent,
        data,
        getWebComponent,
        __mounted,
        $$scope,
        slots,
        rux_segmented_button_binding
    ];
}
class RuxSegmentedButton extends SvelteComponent {
    constructor(options) {
        super();
        init(this, options, instance, create_fragment, safe_not_equal, { data: 3, selected: 0, getWebComponent: 4 });
    }
    $on(type, callback) {
        return super.$on(type, callback);
    }
    $set($$props) {
        super.$set($$props);
    }
    get data() {
        return this.$$.ctx[3];
    }
    set data(data) {
        this.$$set({ data });
        flush();
    }
    get selected() {
        return this.$$.ctx[0];
    }
    set selected(selected) {
        this.$$set({ selected });
        flush();
    }
    get getWebComponent() {
        return this.$$.ctx[4];
    }
}
export default RuxSegmentedButton;
//# sourceMappingURL=RuxSegmentedButton.js.map