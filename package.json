{
  "name": "svelte-wrapped-astro",
  "version": "1.0.0",
  "description": "Astro Components with Svelte Wrapper",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && npm run build:cjs && npm run build:esm",
    "build:cjs": "tsc --module commonjs --outDir dist/cjs",
    "build:esm": "tsc --module es6 --outDir dist/esm"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@astrouxds/astro-web-components": "0.0.12",
    "typescript": "^4.3.5"
  },
  "devDependencies": {
    "svelte": "^3.42.3"
  }
}
